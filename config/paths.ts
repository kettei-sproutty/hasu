import path from 'path'

const SOURCE_PATH = path.join(__dirname, '..', 'src')
const OUTPUT_PATH = path.join(__dirname, '..', 'dist')
const PUBLIC_PATH = '/'

export { SOURCE_PATH, OUTPUT_PATH, PUBLIC_PATH }